import { ProductStockPipe } from './product-stock.pipe';

describe('ProductStockPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductStockPipe();
    expect(pipe).toBeTruthy();
  });
});
